


function convertToNumber(x) {
   try{
    var y = parseFloat(x);
    if( isNaN(y))
        throw "Invalid Numbers";
        if(Number(y));
        console.log(y);
    } catch(err) {
        console.log(err);
    }

     
}

convertToNumber("123");